// Definisi status task dengan Enum
export enum TaskStatus {
    Todo = "TODO",
    InProgress = "IN_PROGRESS",
    CodeReview = "CODE_REVIEW"
}

// definisi jenis task
export enum KindTask {
    Backend = "BACKEND",
    Frontend = "FRONTEND"
}